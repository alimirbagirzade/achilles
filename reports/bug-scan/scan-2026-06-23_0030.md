# Achilles bug-hunt scan — 2026-06-23 (Kademe-2, derin adversarial av)

**Tetikleyici:** Kullanıcı talebi ("tüm projeyi tara + pushla"). f14fd33
(orphan/çok-versiyon kart kürasyonu) sabahki Kademe-1 (09:00) taramasından
SONRA commit'lendiği için o taramada kapsanmamıştı → ana hedef bu yeni kod +
geniş alt-sistem süpürmesi.

## Yöntem
8 alt-sistem finder'ı (read-only, paralel) → her ham bulgu 2-oylu **adversarial
doğrulama** (şüpheci, varsayılan çürütülmüş) → sentez. Çok-ajan workflow
`wzwvplzv1` (49 ajan, ~3.5M token, 535 araç çağrısı).

**Kısıt:** Oturum limiti (03:00 Europe/Istanbul reset) doğrulama fazının sonuna
denk geldi → `synthesize` + 3 alt-sistemin (rag-retrieval, verification-exams,
training-pipeline) adversarial doğrulaması **düştü**. Bu üç bulgu ana-döngüde
elle (doğrudan kod okuması + tetikleme) doğrulandı/sınıflandırıldı. 20 ham bulgu
→ 12 onaylı/olası → dedup sonrası 6 eyleme-değer (1 BLOCKER-sınıf, 1 high, 4 med).

## Kapı (değişiklik sonrası, yeşil)
```
RUFF   : All checks passed!
MYPY   : Success: no issues found in 181 source files
PYTEST : tümü geçti (önceki test_detached_training_stop hatası ÇÖZÜLDÜ)
```

## Bulgular

| # | Bulgu | Dosya | Önem | Durum |
|---|-------|-------|------|-------|
| **A** | `is_detached_training_running(root)` fallback'i `root`'u yok sayıp `_detached_status(get_settings())` ile **gerçek makine** `logs/train-full-err.log` tazeliğini okuyor → ölü-pid testi gerçek taze logu görüp yanlış `True`. (Bilinen "Windows" hatası aslında **izolasyon sızıntısı**; Linux CI'de log yok diye gizliydi.) | `app/training/detached_launch.py:477` | HIGH | ✅ **DÜZELTİLDİ + PUSH** (commit `b99c4a0`, eş zamanlı oturumun launch() commit'ine dahil oldu) |
| **E** | Gate 7 (güvenlik **BLOCKER**) `scan_for_secrets(_card_text(card))` çağırıyor ama `_card_text` `limitations`/`datasets`/`risk_warnings` serbest-metin alanlarını **toplamıyor** → bu alanlardaki sır/PII (API key, e-posta, telefon, TC) BLOCKER'ı atlar. Aynı kör nokta Gate 0/3/5/6'yı da etkiliyor. KnowledgeCard şeması bu 3 alanı `list[str]` olarak içeriyor (knowledge_card_builder.py:46/48/50). | `app/lora/gates.py:82` (`_card_text`) | **BLOCKER-sınıf** | ✅ **DÜZELTİLDİ** (3 alan `_card_text`'e eklendi + 2 regresyon testi) — bu commit |
| **B** | `_extract_trade_returns` blokları **ham `position`** ile sınırlıyor ama `returns` **shift'li** pozisyonla hesaplı → çıkış maliyeti (eff_pos 1→0 geçiş barında, blok bittikten SONRA) per-trade getiriye girmiyor → Kelly tahmini hafif şişiyor. **Headline metrikler (Sharpe/equity) DOĞRU**; yalnız risk_manager Kelly alt-tahmini etkileniyor. | `app/trading/risk_manager.py:267` | MED | ✅ **DÜZELTİLDİ** — bloklar efektif pozisyonla sınırlandı + çıkış-maliyeti barı dahil edildi; 5 yeni test (giriş+çıkış 2 maliyet turu, açık-kuyruk, iki-trade) — bu commit |
| **C** | `concept_graph`/`cross_paper_synthesizer` ingestion-zamanı LLM çağrıları **seedsiz** (kural 6 determinizm). comprehension_scorer doğru seed geçiyor; bu iki ingestion yolu geçmiyor. | `app/research/concept_graph.py`, `cross_paper_synthesizer.py` | MED | ✅ **DÜZELTİLDİ (ikisi de)** — concept_graph `_extract_links` sabit seed (`9453a5a`); cross_paper `_synthesis_seed(block)=int(sha256(block)[:8])` **girdiden-türetilmiş** (reprodüklenebilir AMA girdiye göre çeşitlilik — "hep aynı çıktı" tuzağı yok) ([PR #17](https://github.com/alimirbagirzade/achilles/pull/17) MERGED, +4 test, izole worktree) |
| **D** | `aggregate()` (understanding_score.py:351) skipped/no_data'yı paydadan doğru çıkarıyor ama **minimum-graded koruması YOK** → çevrimdışı tam-merdivende L3/L4 'skipped', yalnız L5 örneği notlanırsa (graded=1) tek geçen sınav pass_rate=%100 + status='scored' → `auto_pipeline` base-vs-adapter **TERFİ kapısını** şişirir (v5-gerileme sınıfı küçük-n sahte güveni; adapter eval `min_n=5` ile aynı). | `app/verification/exams/understanding_score.py:351` | MED | ✅ **DÜZELTİLDİ** — `_MIN_GRADED_FOR_SCORE=3` guard (graded<3 → pass_rate=None + 'insufficient_data'); eşik mevcut 'graded=3→scored' sözleşmesini korur, 7-spec online L3/L4 (≤14) bastırılmaz; +3 assertion test — bu commit |
| safety FN×2 | Güvenlik tarayıcı daraltması (TC checksum + 3-sınıf entropi) 2 yanlış-negatif sınıfı açtı (2-sınıf hex secret, <32 prefix-siz). | `app/lora/safety_scanner.py` | MED | ⏸ **FİX YOK (bilinçli karar)** — derin analizde scanner ZATEN kapsamlı (api_key entropi+sınıf+prefix, PEM private_key, wallet, credential_assignment `password=`, email/phone/national_id); kalan FN'ler **bilinçli FP/FN tradeoff'u** → "düzeltmek" B5'in FP-azaltmasını GERİLETİR (hash'leri tekrar bloklar) + dosya eş zamanlı oturum domaini → gözetimsiz fix YANLIŞ olur |
| memory×3 | Düşük-önem, insan-kapılı, geri-alınabilir davranışlar (blocker değil). | `app/memory/sqlite_store.py` | LOW | skip (tasarım gereği) |
| **G** (churn) | `trading_hypothesis_evaluator` advice-tespiti **olumsuzlama-kör** → "risksiz değildir" / "no strategy is risk-free" gibi alçakgönüllü (kural-1 UYUMLU) hipotezleri naif kelime-eşleşmesiyle HARD-reddediyordu (repro: score=0.8 → 'rejected'). gate_6 precedent'i var. | `app/evals/trading_hypothesis_evaluator.py` | MED | ✅ **DÜZELTİLDİ** — `_advice_present()` ±20 krk olumsuzlama-bilinçli; gerçek "garanti kazandırır" hâlâ reddedilir ([PR #34](https://github.com/alimirbagirzade/achilles/pull/34) MERGED, +3 test) |
| **H** (churn) | `release_gate.check()` YAYIN kapısı: **NaN metrik FAIL-OPEN** (`nan<eşik=False` → bozuk metrik yayını geçiriyor, kural 2) + **None metrik TypeError CRASH**. | `app/reliability/release_gate.py` | MED | ✅ **DÜZELTİLDİ** — None/NaN/inf/bool/non-numeric → fail-closed ([PR #39](https://github.com/alimirbagirzade/achilles/pull/39) MERGED, +8 test) |

## Temiz doğrulanan alt-sistemler (kusur yok)
- **lora-curation** (f14fd33): `plan_curation`/`apply_curation`/`pick_canonical_card_id`
  determinizm + orphan/collapse disjointliği + dry_run varsayılan + idempotenlik
  + ReDoS yok — hepsi sağlam. Geçerli kart yanlışlıkla düşürülmüyor.
- **memory-sql**: SQL enjeksiyonu yok (ORM/bound param), set_card_lora_eligible doğru
  satır (PK), transaction sızıntısı yok (session context commit).
- **trading-core**: look-ahead yok (`position.shift(1)`), maliyet headline'da doğru,
  entropi indikatörleri yalnız geçmiş pencere, Deflated Sharpe bölme-sıfır korumalı,
  eval/exec yok. (Tek istisna = Bulgu B, yalnız per-trade Kelly dekompozisyonu.)
- **web-api**: eğitim onay kapısı SAĞLAM (STOP_ALL + atomik CAS taze-onay, TOCTOU yok),
  kanonik `build_training_split` kullanılıyor, path-traversal korumalı, rate-limit çalışıyor.
- **rag-retrieval**: kural 7 (boş retrieval → 0.0, uydurma yok), RAG loop varsayılan KAPALI
  + eğitimde duruyor, BM25/RRF id-uzayı tutarlı/deterministik. (İstisna = Bulgu C.)
- **verification-exams**: kural 5 — safe_eval ast-whitelist sandbox kaçışı ÜRETİLEMEDİ
  (import/attribute/subscript/lambda/dunder reddediliyor), pow-DoS yok (float overflow).
  (İstisna = Bulgu D.)

## Çözülen bulgular — hepsi origin/main'de (8 fix)
- **Fix A** — detached izolasyon (`b99c4a0`).
- **Fix E** — Gate 7 kör noktası (`ed44bb5`) + 2 test.
- **Fix C-concept** — concept_graph bağ-çıkarımı seed (`9453a5a`).
- **Fix B** — per-trade çıkış maliyeti / Kelly (`a672ce0`) + 5 test.
- **Fix D** — understanding_score min-graded guard (`e4fb9fa`) + 3 test.
- **Fix C-cross** — sentez girdiden-türetilmiş seed (PR #17 MERGED) + 4 test.
- **Fix G** — hypothesis-eval olumsuzlama-bilinçli (PR #34 MERGED) + 3 test.
- **Fix H** — release_gate fail-closed (PR #39 MERGED) + 8 test.

İlk 5 doğrudan main'e push; son 3 **izole worktree + PR** (main working-tree eş zamanlı
oturumla doymuş/ayrışmış olduğundan). Her commit kapı yeşil (ruff + mypy + pytest tümü).

## Tek kalan (gözetimsiz fix YOK — CLAUDE.md)
- **safety FN×2** — bilinçli FP/FN tradeoff + dosya eş zamanlı oturum domaini (yukarı bkz.).

## Ek: churn taraması (yeni-landed PR #12→#41 kodu, read-only — TEMİZ)
2 bug (G/H) dışında ~12 yeni-landed dosya temiz doğrulandı: `metrics.py` (IR Recall/Precision/
MRR/NDCG), `answer_eval.py` (boş-cevap mükemmel-skor almıyor), `rag_ragas_offline.py` (token-örtüşme
proxy, boş→0.0 sahte-pass yok), `regression_runner.py` (tautoloji zaten düzeltilmiş), `source_trust.py`,
`result_verifier.py` (`_is_finite` savunmacı), `retrieval_eval.py`, `eval_runner.py` (G+H fix'leriyle
sağlamlaşan orkestratör), `tool_registry.py` (lazy importlib hardcoded entrypoint=rule-5 OK, requires_seed
zorlanıyor), `probability_simulator.py` (seed zorunlu), `statistics_checker.py` (permütasyon seed-zorunlu),
`registry/` (promotion_gates+version_store: ORM/bound, kural-8 insan-onayı, append-only audit),
`retrieval_audit_log.py` (ORM). **rule-5 (eval/exec): tüm yeni kodda YOK.**
