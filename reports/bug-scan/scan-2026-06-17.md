# Bug-Avı Raporu — 2026-06-17 (Kademe 2, derin adversarial)

> ⚠️ **DURUM: DOĞRULAMA TAMAMLANMADI — bu bulgular DOĞRULANMAMIŞ ADAYLARDIR.**
> Workflow (Run ID `wf_1c29dc1e-f9a`) doğrulama fazında **oturum limitine** takıldı
> (reset 03:30 Europe/Istanbul). Tüm 3-oylu adversarial doğrulayıcılar başlayamadı.
> CLAUDE.md gereği: **doğrulanmamış bulguya gözetimsiz otomatik fix YOK.**
> Fix, doğrulama (resume) sonrası **denetimli** turda yapılacak.

## Kapsam / eksik
- Planlanan 6 alt-sistem; **`backtest` ve `indikator` finder'ları çalışamadı** (oturum
  limiti) → bu iki kritik alan **TARANMADI**, resume'da tekrar koşulmalı.
- Tamamlanan 4 finder: `eval_harness`, `lora_veri`, `rag_sadakat`, `ingestion_determinizm`
  → 16 aday bulgu.
- Doğrulama: yalnız 2 bulguda oy alınabildi (gerisi 0/0 = doğrulanmadı).
- Tam finder gerekçeleri workflow transcript'inde:
  `…/subagents/workflows/wf_1c29dc1e-f9a/agent-*.jsonl`.

## Resume planı (limit sıfırlanınca, ~03:30)
`Workflow({scriptPath: ".../achilles-kademe2-bug-avi-wf_1c29dc1e-f9a.js", resumeFromRunId: "wf_1c29dc1e-f9a"})`
→ tamamlanan finder'lar cache'ten döner; yalnız doğrulayıcılar (+ eksik 2 finder) yeniden koşar.
Sonra ≥2/3 onaylananları **güvenli (WIP-dışı) dosyalarda** düzelt → Kademe-0 kapısı → commit.

---

## Aday bulgular (doğrulanmamış)

Notlar: **WIP** = eşzamanlı oturumun düzenlediği dosya (fix ederken çakışma riski → o tura ait).
Oy = adversarial doğrulayıcı oyu (yes/total); 0/0 = doğrulanmadı.

### eval_harness  (← v5'in kör noktası; en yüksek dikkat)
1. **adapter_eval.py: eval üretimi system-prompt'u atlıyor (train↔eval format uyuşmazlığı)** — `app/training/adapter_eval.py` — oy 0/0
   - ⚠️ DİKKAT: v5-fix'i KASITLI olarak 1/3 örneği system'siz yaptı ve eval'i system'siz çağırdı → bu **yanlış-pozitif olabilir**. Doğrulamada öncelik.
2. **Adapter kaydında yanlış base_model (mlx_base_model, PEFT'in peft_base_model değil)** — `app/lora/auto_pipeline.py` — oy 0/0
3. **success_without_test bayrağı disiplinli olumsuz cevapları yanlış-flag'liyor** — `app/training/evaluate_model.py` — oy 0/0
4. **eval_pass_threshold tanımlı/raporlanıyor ama gate kararında kullanılmıyor (ölü eşik)** — `app/lora/auto_pipeline.py` — oy 0/0

### lora_veri  (← v5 veri reçetesi alanı)
5. **DatasetBuilder.build train/valid ayrımı kaynak-seviyesinde DEĞİL → sahte OOS sızıntısı** — `app/training/dataset_builder.py` — oy 0/0 — **severity: high**
6. **lora-split CLI satır-seviyesi shuffle → kaynak bütünlüğünü atlar, train/valid sızıntısı** — `app/main.py` — oy 0/0 — **WIP**
7. **mix_discipline disiplin satırlarını tabana karşı dedup etmiyor → oranı bozan kopyalar** — `app/training/discipline_dataset.py` — oy 0/0
8. **_significant_numbers binlik-ayraç/yüzde normalizasyonu yanlış eşleşmeye yol açabilir** — `app/brain/synthetic_qa_builder.py` — oy 0/0

### rag_sadakat
9. **RAG cevap üretimi seed'siz (non-determinizm, temperature>0)** — `app/brain/rag_answerer.py` — oy 0/0 — Kural 6
10. **BM25 search berabere skorları set-iterasyon sırasına bağlı (non-determinizm)** — `app/memory/bm25_index.py` — oy 0/0 — Kural 6
11. **Boş grounding sonucunda gnd_score=1.0 — sahte geçme yolu** — `app/learning/rag_exam_runner.py` — oy 0/0 — **severity: high (fake-pass)**
12. **citation_score gerçek atıflardan değil, getirilen chunk'lardan hesaplanıyor** — `app/learning/rag_exam_runner.py` — oy 0/0 — eval dürüstlüğü

### ingestion_determinizm
13. **Embedding↔chunk hizalaması doğrulanmıyor → yanlış vektör/chunk eşleşmesi** — `app/memory/paper_indexer.py` — **oy 0/2 (2 doğrulayıcı ÇÜRÜTTÜ → muhtemelen YANLIŞ-POZİTİF)**
14. **paper_id yalnız 48 bit (file_hash[:12]) → çakışma riski** — `app/ingestion/paper_loader.py` — **oy 1/3 (zayıf, onaylanmadı)**
15. **Math-heavy paragraf chunk_size sınırını atlayıp tek sınırsız chunk oluyor → embedding'de sessiz kesilme** — `app/ingestion/chunker.py` — oy 0/0
16. **find_paper_by_title order_by'sız iterasyon → başlık çakışmasında dedup kazananı non-deterministik** — `app/memory/sqlite_store.py` — oy 0/0 — **WIP**

---

## Öncelik (doğrulama sonrası, kabaca)
- **Yüksek + güvenli dosya:** #5 (OOS sızıntısı), #11 (fake-pass), #12 (citation honesty), #9/#10 (determinizm, Kural 6).
- **Dikkatli (yanlış-pozitif olabilir):** #1 (kasıtlı system'siz eval), #13 (2 çürütme).
- **WIP (eşzamanlı oturuma bırak):** #6 (main.py), #16 (sqlite_store.py).
- **Eksik tarama:** backtest + indikator alt-sistemleri (resume'da koş).
